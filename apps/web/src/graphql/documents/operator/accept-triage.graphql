mutation AcceptTriageMutation($prUrl: String!) {
  acceptTriage(prUrl: $prUrl) {
    id
    prUrl
    githubRepo
    prNumber
    prTitle
    authorLogin
    decision
    effectiveDecision
    decisionLabel
    detail
    kanbanIssueId
  }
}
