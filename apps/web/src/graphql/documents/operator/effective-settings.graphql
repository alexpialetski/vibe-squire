query EffectiveSettingsQuery {
  effectiveSettings {
    coreFields {
      key
      label
      value
      envVar
      description
    }
    resolvedSourceLabel
    resolvedDestinationLabel
    scheduledSyncEnabled
    autoCreateIssues
  }
}
