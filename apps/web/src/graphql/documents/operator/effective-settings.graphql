query EffectiveSettingsQuery {
  effectiveSettings {
    id
    coreFields {
      key
      label
      value
      envVar
      description
    }
    resolvedSourceLabel
    resolvedDestinationLabel
    scheduledSyncEnabled
    autoCreateIssues
  }
}
