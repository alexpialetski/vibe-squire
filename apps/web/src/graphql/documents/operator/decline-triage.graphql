mutation DeclineTriageMutation($prUrl: String!) {
  declineTriage(prUrl: $prUrl) {
    id
    prUrl
    githubRepo
    prNumber
    prTitle
    authorLogin
    decision
    effectiveDecision
    decisionLabel
    detail
    kanbanIssueId
  }
}
