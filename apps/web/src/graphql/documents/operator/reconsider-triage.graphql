mutation ReconsiderTriageMutation($prUrl: String!) {
  reconsiderTriage(prUrl: $prUrl) {
    id
    prUrl
    githubRepo
    prNumber
    prTitle
    authorLogin
    decision
    effectiveDecision
    decisionLabel
    detail
    kanbanIssueId
  }
}
