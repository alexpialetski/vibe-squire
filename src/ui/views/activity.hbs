{{#> layout title="Activity" active="activity" pageScript="operator-activity.js" navMinimal=navMinimal integrationNavEntries=integrationNavEntries}}
  <div class="page">
    <header class="page-head">
      <h1>Activity</h1>
      <p class="muted">Per-sync GitHub PR scout outcomes: created issues, skips, and bots filtered before sync. The list refreshes when the status stream updates; while a run is in progress it also polls briefly so rows update.</p>
    </header>

    <div id="activity-runs-root">
      <div class="card activity-empty-card">
        <p class="muted" style="margin:0">Loading activity&hellip;</p>
      </div>
    </div>
  </div>
{{/layout}}
